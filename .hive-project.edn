{:project-id "hive-proximum"
 :parent "hive"
 :description "Proximum-backed vector store — store + vec protocol/config, init and lifecycle. A loadable storage backend carrying no hive-mcp dependency."
 :language :clojure
 :tags ["storage-backend" "vector-db" "proximum" "spi-impl"]
 :carto {:source-roots ["src" "test"]}}
